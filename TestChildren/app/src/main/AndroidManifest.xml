<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.testchildren">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--<activity-->
            <!--android:name=".SecondScreen"-->
            <!--android:label="SecondScreen"-->
            <!--android:parentActivityName="MainActivity"-->
            <!--android:screenOrientation="portrait"/>-->

        <activity
            android:name=".Letters"
            android:label="Letters"
            android:parentActivityName="MainActivity"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".LetterCards"
            android:label="LetterCards"
            android:parentActivityName="Letters"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".Colors"
            android:label="Colors"
            android:parentActivityName=".MainActivity"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".ColorCards"
            android:label="ColorCards"
            android:parentActivityName="Colors"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".Draw"
            android:label="Draw"
            android:parentActivityName="MainActivity"
            android:screenOrientation="portrait"/>
    </application>

</manifest>